---
name: plan-issue
description: 実装プランをMDファイルの代わりにGitHub Issueとして直接書き起こす。意思決定（検討した選択肢・決定理由）と実装方針を記録する厚いフォーマット。1プラン = 1 Issue。発番されたIssue番号をコミットメッセージに使える。ユーザーが明示的に呼び出した場合のみ使用。
user-invocable: true
disable-model-invocation: true
allowed-tools: Bash(gh *), Bash(git *), Read, Write, Glob, Grep, AskUserQuestion
argument-hint: [プランの概要（省略時は会話の文脈から作成）]
---

# Plan Issue

実装プランを **MDファイルの代わりに GitHub Issue として直接書く**スキル。
プランファイル（`plans/xxx.md` 等）は作らない。Issue そのものがプランの一次ドキュメントになる。

軽い作業メモではなく、**意思決定の過程（検討した選択肢・決定理由）と実装方針**をしっかり記録する Design Doc / RFC 寄りの厚いフォーマットを使う。

想定する開発フロー:

```
1. AIと対話しながらプランを練る
   ↓
2. このスキルでプランをそのままGitHub Issueに書き起こす（1プラン = 1 Issue）
   ↓
3. 実装（発番されたIssue番号をコミットメッセージの #<Issue番号> に使う）
   ↓
4. 完了後にIssueをクローズ。プランは閉じたIssueとして永続的に残る
```

プラン Issue は「作業の手順書」ではなく「**どう作るかの判断の記録**」。closed になった後も意思決定の記録（ADR相当）として機能する。

---

## テンプレート解決の優先順位

Issue本文のフォーマットは以下の優先順位で解決する:

1. **`${CLAUDE_PLUGIN_ROOT}/.plugin-workspace/issues/plan-issue-template.md`**（プロジェクトカスタム）が存在する場合 → その構成を使用
2. **`references/plan-issue-template.md`**（ビルトインデフォルト）→ カスタムが存在しない場合のフォールバック

カスタムテンプレートを作りたい場合は、ビルトインをコピーして `${CLAUDE_PLUGIN_ROOT}/.plugin-workspace/issues/plan-issue-template.md` に配置し、セクションを増減する。

## ワークフロー

```
1. プラン内容の収集（会話の文脈 or 対話ヒアリング）
   ↓
2. テンプレートに沿ってプラン本文を書き起こす
   ↓
3. メタ情報の補完（ベースブランチ・作業ブランチ・関連Issue）
   ↓
4. プレビュー提示とユーザー承認
   ↓
5. gh issue create でIssue登録
   ↓
6. 完了報告（Issue番号とコミットメッセージへの利用案内）
```

### Step 1: プラン内容の収集

**直前の会話でプランを検討していた場合**: その内容（背景、検討した案、決定、実装方針）をそのまま材料にする。あらためてファイルに書き出させたりせず、会話の文脈から直接組み立てる。

**文脈にプランがない場合**: 対話でヒアリングする。最低限、以下を確認する:

1. やること一言（タイトル）
2. 背景・課題
3. 検討した選択肢と決定（選択肢が1つしかない場合はその旨を記録）
4. 実装方針の要点

既存のメモやMDファイルを参照するよう指示された場合は読み込んで材料にしてよいが、ファイルの存在は前提にしない。

### Step 2: テンプレートに沿った書き起こし

解決したテンプレートのセクション構成に従い、プラン本文を組み立てる。

**必須セクション**（情報が欠けている場合はユーザーに確認して補完する）:

| セクション | 内容 |
|:--|:--|
| メタ情報 | ベースブランチ・作業ブランチ・関連Issue/PR |
| 背景 / 課題 | 現状の問題と経緯 |
| ゴール | 達成したいこと（完了条件がわかる粒度） |
| 検討した選択肢 | 案ごとの概要・良い点・悪い点 |
| 決定 | 採用案と理由（トレードオフの受け入れを明記） |
| 実装方針 | 全体設計・インターフェース設計・変更対象と影響範囲・実装の順序・採用する規約 |
| 検証方法 | どう確認したら完了とみなすか |

**任意セクション**（情報がなければ省略してよい）:

| セクション | 内容 |
|:--|:--|
| 非ゴール（スコープ外） | 今回やらないこと |
| リスクと対策 | 想定リスクと緩和策 |
| 参考資料 | 調査リンク・関連ADR・参考実装 |

**書き起こしのルール:**

- チェックリスト形式の「実装ステップ（作業手順）」は含めない。手順は実装時に判断する。「実装の順序」は着手順の方針レベルにとどめる
- 検討過程が会話に残っていない（結論だけある）場合、「検討した選択肢・却下理由」をヒアリングして補完する。不明なら「案A（採用）のみ検討」と正直に記録する
- 会話で決まった内容を勝手に要約で削らない。決定理由や却下理由の創作もしない

### Step 3: メタ情報の補完

```bash
# ベースブランチの推定（デフォルトブランチ）
BASE_BRANCH=$(gh repo view --json defaultBranchRef --jq '.defaultBranchRef.name')

# 作業ブランチの推定（現在のブランチ。ベースブランチと同一なら未定として扱う）
WORK_BRANCH=$(git branch --show-current)
```

- ベースブランチ: 会話で明示されていればそれを優先。なければ上記の推定値を提示して確認
- 作業ブランチ: 未定の場合は空欄にせず「未定（実装時に決定）」と記録してよい
- 関連 Issue / PR: 会話に出ていれば転記。なければ「なし」

### Step 4: プレビューとユーザー承認

組み立てたIssueタイトル・ラベル・本文の全文をユーザーに提示し、承認を得る。
修正指示があれば反映して再提示する。**承認なしにIssueを作成しない。**

### Step 5: Issue登録

```bash
PLAN_URL=$(gh issue create \
  --title "[Plan] {やること一言}" \
  --body "{テンプレートに従って組み立てた本文}" \
  --label "type:plan")

PLAN_NUM=$(echo "$PLAN_URL" | grep -oE '[0-9]+$')
```

- ラベルは `type:plan` を必ず付与する。内容から判定できる場合は `area:*` / `size:*` ラベルも付与する（判定基準は `spec-to-issues` スキルと同一）
- `type:plan` ラベルが存在しない場合は `scripts/create-github-labels.sh` の実行を案内する

### Step 6: 完了報告

```
## プランIssue登録完了

- #{番号} [Plan] {タイトル}
- URL: {issue_url}

コミット時はこの番号を使用してください:
  <emoji> [<tag>]: #{番号} <subject>
```

## エラーハンドリング

| 状況 | 対応 |
|:--|:--|
| 会話にプランの材料が不足 | ユーザーにヒアリングして補完（勝手に創作しない） |
| 参照指示されたファイルが見つからない | エラー報告、正しいパスを確認 |
| カスタムテンプレートが不正 | 報告してビルトインにフォールバック |
| `gh` CLIが未認証 | `gh auth login` の実行を案内 |
| `type:plan` ラベルが未作成 | `create-github-labels.sh` の実行を案内 |
| Issue作成APIエラー | エラー報告、1回リトライ、それでも失敗ならユーザーに確認 |
| 同名タイトルのopenなプランIssueが既に存在 | 重複の可能性を提示し、続行するか確認 |

## 禁止事項

- ユーザーの承認なしにIssueを作成しない
- プランファイル（MD）を新たに作成しない（Issue が一次ドキュメント。中間ファイルも作らない）
- 会話で決まっていない決定理由や検討過程を創作しない
- `gh issue create` 以外の方法でIssueを作成しない
